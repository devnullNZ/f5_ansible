---
all:
  children:
    f5:
      hosts:
        bigip1:
          ansible_host: 192.168.80.130

    # Subset of f5 hosts that are the *primary* GTM in their sync group.
    # The GSLB play targets this group only; GTM sync propagates writes
    # to the standby GTM peer(s).
    f5_gtm_primary:
      hosts:
        bigip1:
